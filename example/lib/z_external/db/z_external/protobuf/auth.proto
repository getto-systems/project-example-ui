syntax = "proto3";

message AuthnMessage {
  string nonce = 1;
  string authAt = 2;
}

message AuthzMessage {
  string nonce = 1;
  repeated string roles = 2;
}

message MenuExpandMessage {
  message Path { repeated string labels = 1; }

  repeated Path paths = 1;
}
